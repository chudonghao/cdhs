extern double sin(double);

int main(int argc, char *argv[]) {
  int v1;
  int v2 = 1;
  float v3;
  float v4 = 1.;
  float v5 = 2.;

  v1 = v2 * v2 + v4 * v5;

  v1 = v2 + sin(v4);

  if (v1 + v2) {
    int v1;
  }
  if (v1 + v2)
    int v1;

  for (int i = 0, j; i < 10; ++i, ++j) {
    int v1;
  }

  for (int i = 0, j; i < 10; ++i, ++j)
    int v1;

  return 0;
}
